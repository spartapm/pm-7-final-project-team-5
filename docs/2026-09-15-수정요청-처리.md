# 5조 2026.09.15 수정 요청사항 — 처리 결과

기준: 피그마 [5조 · 2026.09.15 - 수정 요청사항](https://www.figma.com/design/4vkt1I8oKaDETSNRHXF4ns/5%EC%A1%B0?node-id=3349-79)  
정책: `docs/인사이트 카드 발행 정책.md` 3-3-0 (빨간 표시 프롬프트)

요청 카피가 있는 항목만 반영했다.

| 표시 | 의미 |
| --- | --- |
| ✅ | 요청대로 반영 |
| ❌ | 안 함 (요청이 유지이거나 해당 화면 없음) |

---

## 온보딩

| 요청 | 상태 | 처리 |
| --- | --- | --- |
| 항목 3) 인사이트 리포트 원본 750px, 우측 첨부 이미지 사용 (1순위) | ✅ | 09.14에서 넣은 `insight-report-2x.png`(750×3038)를 피그마 프레임처럼 상단 크롭해 750×2666만 노출. 첨부 레이어와 동일 원본 |
| 항목 4) 0-8b narrative2 볼드, narrative1은 유지 (2순위) | ✅ | 09.14의 동일 크기 지정을 **09.15가 뒤집어** n2 `15px/700`, n1 `13px/400` |

---

## 홈 / 계획

| 요청 | 상태 | 처리 |
| --- | --- | --- |
| 2-1 홈 `문의하기` → 인스타그램 (1순위) | ✅ | `LegalFooter` 링크를 `https://www.instagram.com/inplot_official/` (`target=_blank`). 설치된 기기에서는 인스타 앱으로 열림 |

---

## 인사이트

| 요청 | 상태 | 처리 |
| --- | --- | --- |
| 대시보드 탭 미리보기 이미지 교체 | ✅ | 기록 0건 캐러셀을 피그마 TO-BE 5장 순서로 교체: 매매 추이 → 계획 이행 → 한눈에 보기 → 자세히 보기 → TOP 3. `public/figma/preview/` |
| narrative1 LLM 프롬프트 | ✅ | 정책서 규칙 6(필수 어미를 글자 그대로 붙이지 말 것) + 규칙 9(반복·문법 자가점검)를 `app/api/insight-copy/route.ts`에 반영. 필수 어미에서 `한` 제거 (`경향이 보여요` / `패턴이 반복되고 있어요`) |
| narrative2 LLM 프롬프트 | ✅ | 규칙 4(어미 교체), 7(트리거+당시상태 필수, 숫자 금지), 10(문법 자가점검). few-shot에서 `3건 중 2건` 문구 삭제 |

---

## 이미지 재요청 메모

온보딩 리포트·인사이트 미리보기는 09.11/09.14에서도 반복된 항목이다. 이번에는 피그마가 붙여 준 PNG를 그대로 캐러셀에 넣고, 온보딩 리포트는 첨부 750px 원본을 크롭만 해서 쓴다.
